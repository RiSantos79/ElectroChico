API_URL=http://localhost:3333
JWT_SECRET="tem de ser exatamente igual ao JWT_SECRET da api/.env"

# Opcional — só em staging, para bloquear o acesso a quem não tiver a credencial.
# Deixa por preencher em desenvolvimento local.
STAGING_BASIC_AUTH_USER=
STAGING_BASIC_AUTH_PASSWORD=
